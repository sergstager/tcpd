сборка - make all

после чего на разных консолях:
на одной: make test
на другой: make t

в результате файл /vmlinuz перекинется в /usr/local/tmp/ через сеть под случайным
именем. запуск команды ещё не сделан

вопросы: 

1) как в конфиге прописывать внешнюю команду? нужно определиться как будут
опции задаваться в конфиге: путь до файла (или можно искать команду в $PATH?),
аргументы (в каком виде? "-c -v -f %s -d")?

2) что логгировать? вот в таком виде:

низкий уровень:
 - старт демона
 - стоп демона
средний уровень:
 - получение файла
 - запуск команды
 - получение внешнего сигнала
высший уровень:
 - разбор конфига
 - форк для обработки входящего сокета

3) в случае если не задан в командной строке путь до конфига, искать его
гденибудь (/etc, /usr/local/etc, ~/.tcpdaemon.cfg) или просто завершаться?

4) делать поиск уже запущенной копии демона? и что делать если она находится
(завершать работающую, завершать запускающуюся, запускать две копии?)

5) тестирование на удалённых машинах с обрывающимися каналами и всякими
другими возможными ужасами надо наверное?

6) кто подчищает временные файлы? демон после завершения команды (я даже
сейчас не смогу сказать, есть у execv хук на завершение подпроцесса, так что не
уверен, что это возможно) или это уже дело самой команды?

7) что делать с ошибками? просто выдавать номер ошибки? выдавать на русском?
