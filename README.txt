сборка - make all
установка в /usr/local/ - make install

после чего на разных консолях:
на одной: make test (завершить можно по ctrl-c)
на другой: make t

в результате файл /vmlinuz перекинется в /usr/local/tmp/ через сеть под случайным
именем и запустится команда /usr/local/bin/extern_comm с заданными в конфиге аргументами
